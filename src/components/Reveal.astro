---
interface Props {
  as?: keyof HTMLElementTagNameMap;
  class?: string;
  id?: string;
  [key: string]: any;
}
const { as: Tag = 'div', class: className = '', id, ...rest } = Astro.props;
---
<Tag id={id} class={`reveal ${className}`} {...rest}><slot /></Tag>
