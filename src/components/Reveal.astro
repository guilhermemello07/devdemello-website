---
interface Props {
  as?: keyof HTMLElementTagNameMap;
  class?: string;
  id?: string;
}
const { as: Tag = 'div', class: className = '', id } = Astro.props;
---
<Tag id={id} class={`reveal ${className}`}><slot /></Tag>
